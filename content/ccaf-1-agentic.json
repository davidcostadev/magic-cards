{
  "subjects": [
    {
      "id": "ccaf-1-agentic",
      "title": "CCAF 1 - Agentic Architecture",
      "description": "Domain 1 (27%) - the agentic loop, stop_reason handling, multi-agent orchestration patterns, and the three anti-patterns.",
      "color": "#D97757",
      "icon": "workflow"
    }
  ],
  "cards": [
    {
      "id": "ccaf1-open-001",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "Describe the full agentic loop lifecycle with the Messages API.",
      "answer": "1. **Call** `POST /v1/messages` with `messages`, `tools`, and a system prompt.\n2. **Inspect `stop_reason`**. If it is `end_turn`, the turn is done. If it is `tool_use`, the model wants a tool.\n3. **Append the assistant turn verbatim** - the whole `response.content` array, including every `tool_use` block (and any `thinking` blocks).\n4. **Execute** each requested tool in your own code.\n5. **Append a `user` message** whose content is only `tool_result` blocks, each carrying the matching `tool_use_id`.\n6. **Re-invoke** the API with the grown `messages` array.\n7. Repeat from step 2 until `stop_reason` is no longer `tool_use`.\n\nThe loop is driven by **`stop_reason`**, not by parsing text. Your code owns execution and I/O; the model owns the decision of what to call next.\n\nThe shape of the loop:\n\n```mermaid\ngraph TD\n  A[\"Call POST /v1/messages\"] --> B{\"stop_reason?\"}\n  B -->|end_turn| Z[\"Turn is done\"]\n  B -->|tool_use| C[\"Append the assistant turn verbatim\"]\n  C --> D[\"Execute each tool in your own code\"]\n  D --> E[\"Append a user turn of tool_result blocks\"]\n  E --> A\n```",
      "tags": ["agentic-loop", "messages-api"],
      "hints": [
        "Four moving parts: the call, the one field you branch on, what you append, and the fact that you go back to step two."
      ]
    },
    {
      "id": "ccaf1-open-002",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "After a response comes back with `stop_reason: \"tool_use\"`, exactly what do you append to `messages` before the next call?",
      "answer": "Two messages, in order:\n\n1. The **assistant** message containing the model's **entire** `content` array, unmodified. Do not drop the text blocks, do not drop `thinking` blocks, do not rewrite the `tool_use` blocks.\n2. A **user** message whose `content` is a list of `tool_result` blocks - **and nothing else**. Each block needs `tool_use_id` matching the `tool_use.id` it answers.\n\n```json\n{ \"role\": \"user\", \"content\": [\n  { \"type\": \"tool_result\", \"tool_use_id\": \"toolu_01A\", \"content\": \"72F, clear\" }\n]}\n```\n\nAdding trailing text after the `tool_result` blocks is a documented mistake: it can produce an empty or degraded response. If several `tool_use` blocks came back in one turn, **all** their results go in that single user message.",
      "hints": ["Two messages - and the second one is allowed to contain only one kind of block."],
      "tags": ["agentic-loop", "tool-result"]
    },
    {
      "id": "ccaf1-open-003",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "Anti-pattern 1: why is natural-language termination (\"stop when the model writes DONE\") wrong?",
      "answer": "Because it makes the **control flow depend on free-form text**, which is not a contract.\n\n- The model can produce the sentinel inside an explanation, a quote, or a code sample and halt a healthy run early.\n- It can finish correctly and phrase it differently (\"All set!\"), so the loop never terminates.\n- Translation, streaming, and formatting changes silently break the parser.\n\nThe API already gives you a **typed, structured** signal for this: `stop_reason`. `end_turn` means the model is finished; `tool_use` means it needs a tool. Branch on that field. If you need a richer structured verdict, make the model call a `submit_final_answer`-style tool so the completion signal is a `tool_use` block with a validated schema, not prose.",
      "tags": ["anti-patterns", "termination"],
      "hints": ["The API already hands you a typed field for this. Prose is not a contract."]
    },
    {
      "id": "ccaf1-open-004",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "Anti-pattern 2: why is an arbitrary iteration cap not a termination strategy?",
      "answer": "A cap like `for i in range(10)` is a **safety backstop**, not a definition of \"done\". Used as the primary control it fails in both directions:\n\n- **Too low**: a legitimately long task is cut off mid-work and returns a partial answer that looks complete.\n- **Too high**: a stuck agent burns tokens and latency for the full budget before anyone notices.\n- It hides the real bug: an agent that cannot converge usually has a bad tool description, a missing tool, or an unclear goal.\n\nCorrect design: terminate on **`stop_reason`**, and keep a cap only as a circuit breaker that, when tripped, is treated as a **failure path** - log it, emit a metric, and escalate to a human. A silent `break` that returns whatever was accumulated is the dangerous version.",
      "tags": ["anti-patterns", "termination", "reliability"],
      "hints": [
        "It fails in both directions - too low and too high - and it hides the real bug underneath."
      ]
    },
    {
      "id": "ccaf1-open-005",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "Anti-pattern 3: why is \"the response contains a text block\" not a completion signal?",
      "answer": "Because a single assistant turn can contain **both** text and `tool_use` blocks. The model routinely narrates before it acts:\n\n```json\n\"content\": [\n  { \"type\": \"text\", \"text\": \"Let me look up that order.\" },\n  { \"type\": \"tool_use\", \"name\": \"get_order\", \"id\": \"toolu_01A\", \"input\": {...} }\n]\n```\n\nIf you treat the presence of text as \"the agent answered\", you return the narration to the user, never execute `get_order`, and the conversation is left in an invalid state (an assistant `tool_use` with no matching `tool_result`), which breaks the next request.\n\nThe rule: **`stop_reason` decides whether the turn is over; the content array only tells you what is in it.** Iterate the array to find the blocks you need, but branch on `stop_reason`.",
      "tags": ["anti-patterns", "termination"],
      "hints": ["A single assistant turn can hold more than one kind of block at the same time."]
    },
    {
      "id": "ccaf1-open-006",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "When should control flow be model-driven, and when should it be a pre-configured sequence?",
      "answer": "The distinction the exam draws is **who decides the next step**:\n\n- **Model-driven**: Claude reasons about which tool to call next based on context. Right when the number and order of steps depend on what is discovered while working.\n- **Pre-configured**: a decision tree or a fixed tool sequence written by you. Right when the steps are known in advance - it is cheaper, testable, and traceable.\n\nThe same choice appears one level up, in **task decomposition**:\n\n| Shape | Use when |\n|---|---|\n| **Fixed sequential pipeline** (prompt chaining) | predictable multi-aspect work, e.g. analyze each file, then run a cross-file integration pass |\n| **Dynamic adaptive decomposition** | open-ended investigation, where each step's findings generate the next subtasks |\n\nExample of the second: \"add comprehensive tests to a legacy codebase\" - you first map structure, then identify high-impact areas, then build a prioritized plan that adapts as dependencies are discovered.",
      "tags": ["agentic-loop", "task-decomposition"],
      "hints": ["Ask who picks the next step - and whether the steps are knowable up front."]
    },
    {
      "id": "ccaf1-open-007",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "Describe the coordinator-subagent (hub-and-spoke) architecture and what the coordinator owns.",
      "answer": "One **coordinator** agent sits at the hub and manages **all** inter-subagent communication, error handling, and information routing. Subagents talk only to the coordinator.\n\nThe coordinator owns:\n- **Task decomposition** and delegation\n- **Deciding which subagents to invoke** based on query complexity, rather than always running the full pipeline\n- **Result aggregation** and synthesis\n- **Scope partitioning** across subagents to minimize duplication (distinct subtopics or source types per agent)\n\nSubagents run with **isolated context** - they do not inherit the coordinator's conversation history, so everything they need must be passed explicitly in their prompt.\n\nWhy route everything through the hub: **observability**, consistent error handling, and controlled information flow. The failure mode to watch is the opposite of complexity - **overly narrow decomposition**, which leaves a broad research topic only partly covered.\n\n```mermaid\ngraph TD\n  Q[\"Query\"] --> C[\"Coordinator: decompose, select, route\"]\n  C --> S1[\"Search subagent\"]\n  C --> S2[\"Analysis subagent\"]\n  C --> S3[\"Synthesis subagent\"]\n  S1 --> C\n  S2 --> C\n  S3 --> C\n  C --> R[\"Report\"]\n```",
      "tags": ["multi-agent", "coordinator", "hub-and-spoke"],
      "hints": ["Everything crosses the hub. Ask what that buys you when something fails."]
    },
    {
      "id": "ccaf1-open-008",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "The coordinator's synthesis comes back with gaps in coverage. What loop does it run?",
      "answer": "An **iterative refinement loop**, driven by the coordinator:\n\n1. Evaluate the synthesis output **for gaps** - which subtopics are thin or unsupported.\n2. **Re-delegate** to the search and analysis subagents with **targeted** queries aimed at those specific gaps.\n3. **Re-invoke synthesis** with the new findings.\n4. Repeat until coverage is sufficient.\n\nThe point is that the coordinator is the only participant that can see the whole picture, so gap detection has to live there - a subagent cannot know what the others missed.\n\nNote what this is not: it is not one generator arguing with one judge in the abstract. The evaluation criterion is concrete - **coverage of the original research question** - and the loop terminates on that, not on a subjective quality verdict.",
      "tags": ["multi-agent", "iterative-refinement", "coordinator"],
      "hints": ["Only one participant can see what everybody else missed."]
    },
    {
      "id": "ccaf1-open-010",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "What is prompt chaining and what does it trade away?",
      "answer": "Decomposing a task into a **fixed sequence** of LLM calls, where each call's output is the next call's input, often with a programmatic gate (\"check\") between steps. Example: generate marketing copy -> verify it meets the brief -> translate it.\n\nIt **trades latency for accuracy**: more round trips, but each call has one narrow job, so each is more reliable, and you can validate between steps instead of only at the end.\n\nUse it when the task decomposes cleanly into steps you know in advance. If you cannot name the steps up front, you want routing, orchestrator-workers, or an agent instead.\n\nA fixed sequence, with a gate between steps:\n\n```mermaid\ngraph LR\n  A[\"Call 1: generate copy\"] --> G{\"Gate: meets the brief?\"}\n  G -->|no| A\n  G -->|yes| B[\"Call 2: translate\"]\n  B --> R[\"Output\"]\n```",
      "tags": ["patterns", "prompt-chaining"],
      "hints": [
        "Fixed steps with checks between them. Something gets worse so accuracy can get better."
      ]
    },
    {
      "id": "ccaf1-open-011",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "Why should a coordinator not always route every query through the full subagent pipeline?",
      "answer": "Because pipeline depth should follow **query complexity**. The skill the exam names is designing coordinators that **analyze query requirements and dynamically select which subagents to invoke**, instead of always running search -> analysis -> synthesis -> report.\n\nA simple factual lookup that goes through four subagents pays four delegations, four context transfers, and a synthesis pass to answer something one search would have settled.\n\nThe paired skill is **scope partitioning**: when you do fan out, assign each subagent a distinct subtopic or source type so they do not duplicate each other's work.\n\nAnd the counterweight: decompose too narrowly and a broad topic ends up only partly covered. Selection is a judgement about the query, not a fixed rule.",
      "tags": ["multi-agent", "coordinator", "cost"],
      "hints": ["The pipeline has a fixed cost. Does every query deserve all of it?"]
    },
    {
      "id": "ccaf1-open-013",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "How do you design human-in-the-loop escalation inside an agentic system?",
      "answer": "Escalation is a **first-class path**, not an error fallback. Design it as:\n\n1. **Explicit triggers** you can enumerate: low model confidence, a policy/PII/compliance category, an action above a value threshold (refund > $X), repeated tool failures, the iteration circuit breaker tripping, or the user asking for a human.\n2. **A tool, not a prose convention**: give the model an `escalate_to_human` tool with a schema (`reason`, `summary`, `urgency`). Then escalation arrives as a typed `tool_use` block you can route, meter, and test - not a sentence you have to parse.\n3. **Handoff context**: pass the conversation summary and what was already tried, so the human does not restart from zero.\n4. **Approval gates** for irreversible actions - the agent proposes, a human confirms, then the tool executes.\n\nThe symmetric failure modes are escalating everything (no value delivered) and never escalating (the agent improvises on a case it cannot handle).\n\nEscalation as a designed path:\n\n```mermaid\ngraph TD\n  T[\"Agent turn\"] --> C{\"Escalation trigger?\"}\n  C -->|no| N[\"Continue the loop\"]\n  C -->|\"confidence, policy, threshold, repeated failures, user asks\"| E[\"escalate_to_human tool call\"]\n  E --> H[\"Handoff: summary plus what was already tried\"]\n  H --> A{\"Irreversible action?\"}\n  A -->|yes| G[\"Agent proposes, human confirms, then the tool executes\"]\n  A -->|no| K[\"Human takes it from here\"]\n```",
      "tags": ["human-in-the-loop", "escalation", "reliability"],
      "hints": ["Make it a typed thing the model can call, and enumerate what sets it off."]
    },
    {
      "id": "ccaf1-open-014",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "How do you carry state across turns and across work sessions in an agent workflow?",
      "answer": "**Within a conversation**: every request carries the **complete conversation history** - the API keeps nothing between calls, and dropping history breaks conversational coherence. Keep `tool_use`/`tool_result` pairs intact.\n\n**Across sessions**, three named mechanisms:\n\n- **`--resume <session-name>`** - continue a specific named prior conversation.\n- **`fork_session`** - branch from a shared analysis baseline to explore divergent approaches (e.g. comparing two refactoring strategies without re-analyzing the codebase).\n- **A fresh session with an injected summary** - the more reliable choice when the prior session's **tool results are stale**.\n\nThat last trade-off is the exam's point: resume when prior context is **mostly still valid**; start fresh with a structured summary when it is not. And when you do resume after code changed, **tell the agent which files changed** so it re-analyzes those instead of everything.\n\nFor long explorations, **scratchpad files** persist key findings across context boundaries, and structured state exports (**manifests**) let a coordinator recover after a crash.",
      "tags": ["sessions", "state", "context"],
      "hints": ["Two flags and one deliberate decision to throw the old session away."]
    },
    {
      "id": "ccaf1-open-015",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "Why use subagents, and what is the main architectural benefit?",
      "answer": "**Context isolation.** Each subagent runs with its own context window and its own system prompt, does a bounded piece of work (search a subsystem, review one dimension, verify one claim), and returns a **compact result** to the parent. The parent's context grows by the summary, not by everything the subagent had to read.\n\nSecondary benefits: parallelism (independent subagents run concurrently), specialization (a narrower prompt and a smaller toolset per role), and least-privilege (a read-only researcher gets no write tools).\n\nThe cost: each subagent starts with **no memory of the parent conversation**, so the spawn prompt must be self-contained. Subagents are also poor for tightly coupled work that needs constant back-and-forth - the handoff overhead exceeds the benefit.",
      "tags": ["multi-agent", "subagents", "context"],
      "hints": ["The main benefit is about context windows, not about speed."]
    },
    {
      "id": "ccaf1-open-016",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "What guardrails belong around an autonomous agent that takes real actions?",
      "answer": "- **Sandbox the environment** - containers, scoped credentials, a test database - and test extensively there before production.\n- **Least privilege on tools**: expose only what the task needs; separate read tools from write tools; require an approval gate for destructive or irreversible ones.\n- **Circuit breakers**: max iterations, max tokens, max wall-clock, max spend per session. Tripping one is a failure that escalates, not a silent return.\n- **Validate tool inputs in your code** - never trust that the model's arguments are in range just because the schema says so.\n- **Idempotency keys** on side-effecting tools, because retries and loops will re-issue calls.\n- **Transparency**: log every step (the plan, each `tool_use`, each result) so a run can be replayed and audited.\n\nThe framing from *Building effective agents*: agents are for **scaling tasks in trusted environments**; the trust is something you engineer, not assume.",
      "tags": ["guardrails", "reliability", "security"],
      "hints": [
        "Least privilege, circuit breakers, a sandbox - plus what happens the moment a breaker trips."
      ]
    },
    {
      "id": "ccaf1-open-019",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "The model returns three `tool_use` blocks in one assistant turn. How do you respond?",
      "answer": "Execute all three - **in parallel if they are independent** - and return **all three `tool_result` blocks in a single user message**, each with its matching `tool_use_id`.\n\n```json\n{ \"role\": \"user\", \"content\": [\n  { \"type\": \"tool_result\", \"tool_use_id\": \"toolu_01\", \"content\": \"...\" },\n  { \"type\": \"tool_result\", \"tool_use_id\": \"toolu_02\", \"content\": \"...\" },\n  { \"type\": \"tool_result\", \"tool_use_id\": \"toolu_03\", \"content\": \"...\" }\n]}\n```\n\nSending them one per message, or answering only the first, leaves unmatched `tool_use` blocks and produces an API error. Parallel tool calls are one of the cheapest latency wins in an agent loop - a turn that needed three sequential round trips becomes one.",
      "tags": ["tool-use", "parallel-tools", "agentic-loop"],
      "hints": ["How many user messages do you send back?"]
    },
    {
      "id": "ccaf1-open-020",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "A tool call fails. What should the tool return so the agent can recover well?",
      "answer": "Not a generic failure. Return the MCP **`isError`** flag plus **structured error metadata**:\n\n```json\n{ \"isError\": true,\n  \"errorCategory\": \"transient\",\n  \"isRetryable\": true,\n  \"description\": \"Inventory service timed out after 5s.\" }\n```\n\nCategorize, because the right recovery differs:\n\n| Category | Example | Agent's move |\n|---|---|---|\n| **transient** | timeout, service unavailable | retry, possibly after a delay |\n| **validation** | invalid input | fix the argument and call again |\n| **business** | policy violation | explain it to the user; `retriable: false` |\n| **permission** | caller not authorized | stop and escalate |\n\nWhy uniform responses hurt: a generic \"Operation failed\" leaves the agent unable to make an appropriate recovery decision, so it retries what will never succeed and gives up on what would.\n\nAlso distinguish an **access failure** (needs a retry decision) from a **valid empty result** (a successful query with no matches) - collapsing those two is a classic bug. In a multi-agent system, subagents should attempt **local recovery** for transient failures and propagate only what they cannot resolve, **with partial results and what was attempted**.",
      "tags": ["error-handling", "tool-design", "error-category"],
      "hints": ["Four categories, one boolean. Generic failure messages are the anti-pattern."]
    },
    {
      "id": "ccaf1-quiz-001",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "You are building a customer support agent. Which signal should drive the loop's decision to continue or stop?",
      "answer": "`stop_reason` is the API's typed control signal. `tool_use` means continue the loop; `end_turn` means the turn is complete. Everything else - text content, sentinel words, iteration counters - is either unreliable or a backstop.",
      "choices": [
        {
          "id": "a",
          "text": "The `stop_reason` field on each response",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Whether the response contains a text block",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Whether the model wrote a termination phrase such as TASK COMPLETE",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "A fixed maximum iteration count",
          "isCorrect": false
        }
      ],
      "tags": ["agentic-loop", "anti-patterns"],
      "hints": [
        "One of these is a typed field on every response. The others are inferences you are making."
      ]
    },
    {
      "id": "ccaf1-quiz-002",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A response has `stop_reason: \"tool_use\"`. What is the correct next message sequence?",
      "answer": "Append the assistant's full content array (with its `tool_use` blocks), then a user message containing only `tool_result` blocks with matching `tool_use_id`s. Adding text after the `tool_result` blocks is explicitly discouraged and can produce degraded responses.",
      "choices": [
        {
          "id": "a",
          "text": "Append the assistant content, then a user message containing only `tool_result` blocks",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Append a user message with the tool output as plain text",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Append a user message with the `tool_result` blocks followed by a text block explaining them",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Append an assistant message containing the `tool_result` blocks",
          "isCorrect": false
        }
      ],
      "tags": ["agentic-loop", "tool-result"],
      "hints": ["Watch what is allowed to appear inside the follow-up user message."]
    },
    {
      "id": "ccaf1-quiz-003",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A research pipeline must break a question into sub-questions that depend on the question itself, delegate each, and merge the findings. Which pattern is this?",
      "answer": "Orchestrator-workers. The decomposition happens at runtime and is decided by the model, which is exactly what separates it from parallelization/sectioning, where the split is hardcoded by your application.",
      "choices": [
        {
          "id": "a",
          "text": "Orchestrator-workers",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Parallelization with sectioning",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Prompt chaining",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Evaluator-optimizer",
          "isCorrect": false
        }
      ],
      "tags": ["patterns", "multi-agent"],
      "hints": ["Who decided on the sub-questions - your code in advance, or the model at runtime?"]
    },
    {
      "id": "ccaf1-quiz-005",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A research coordinator runs search, analysis, synthesis, and report subagents on every query, including simple factual lookups. What is the best change?",
      "answer": "Pipeline depth should follow query complexity. The named skill is a coordinator that analyzes the query and dynamically selects which subagents to invoke, rather than always routing through the full pipeline.",
      "choices": [
        {
          "id": "a",
          "text": "Have the coordinator analyze query requirements and select only the subagents that query needs",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Run the four subagents in parallel so the total latency stops being additive",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Give every subagent the full tool set so any of them can finish the query alone",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Raise the coordinator's turn limit so it can re-delegate whenever output looks thin",
          "isCorrect": false
        }
      ],
      "tags": ["multi-agent", "coordinator"],
      "hints": ["A factual lookup does not need a synthesis pass over one document."]
    },
    {
      "id": "ccaf1-quiz-006",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Which task calls for dynamic adaptive decomposition rather than a fixed prompt-chaining pipeline?",
      "answer": "Fixed sequential pipelines fit predictable multi-aspect work. Dynamic adaptive decomposition fits open-ended investigation, where the subtasks are generated by what earlier steps discover - map structure, find high-impact areas, then build a plan that adapts.",
      "choices": [
        {
          "id": "a",
          "text": "Adding comprehensive tests to a legacy codebase, where findings reveal the next dependency",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Reviewing a pull request for security, correctness, style, and test coverage in turn",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Extracting the same twelve fields from every invoice arriving in the nightly batch",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Analyzing each changed file, then running one cross-file integration pass at the end",
          "isCorrect": false
        }
      ],
      "tags": ["task-decomposition"],
      "hints": ["Three of these you could write the steps for before starting."]
    },
    {
      "id": "ccaf1-quiz-007",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Your agent loop breaks out after 10 iterations and returns whatever text it accumulated. What is the main problem?",
      "answer": "A cap is a circuit breaker, so tripping it means the agent failed to converge. Returning the partial state as if it were an answer hides the failure from users and from your monitoring. The cap should raise an error and escalate.",
      "choices": [
        {
          "id": "a",
          "text": "Hitting the cap is a failure, but it is reported as a successful answer",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Ten iterations is too few for any real agent",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Iteration caps are forbidden in agentic systems",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "The loop should count tool calls instead of iterations",
          "isCorrect": false
        }
      ],
      "tags": ["anti-patterns", "reliability"],
      "hints": ["Ask what hitting the cap actually tells you about the run."]
    },
    {
      "id": "ccaf1-quiz-008",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A `get_inventory` tool times out. What should it return to the agent?",
      "answer": "Structured metadata lets the agent decide correctly: a transient, retryable failure should be retried. An empty result reads as \"no inventory exists\" and produces a confident wrong answer; a generic string tells the agent nothing it can act on; terminating the workflow throws away every other subagent's work.",
      "choices": [
        {
          "id": "a",
          "text": "A structured error with `errorCategory: transient`, `isRetryable: true`, and a readable description",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "An empty result set, so the agent treats the lookup as complete and moves on",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "A generic `\"Operation failed\"` string, keeping the internal failure detail private",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Nothing at all, letting the coordinator's timeout handler terminate the whole workflow",
          "isCorrect": false
        }
      ],
      "tags": ["error-handling", "error-category"],
      "hints": [
        "The agent has to choose between retrying and giving up. What does it need to choose well?"
      ]
    },
    {
      "id": "ccaf1-quiz-011",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A turn returns four `tool_use` blocks. What is the correct handling?",
      "answer": "Execute all four - in parallel when they are independent - and return all four `tool_result` blocks in one user message. Leaving any `tool_use` without a matching `tool_result` makes the next request invalid.",
      "choices": [
        {
          "id": "a",
          "text": "Execute all four and return all four `tool_result` blocks in one user message",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Execute the first one and re-invoke; the model will ask for the rest again",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Send one user message per `tool_result`, in order",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Merge the four outputs into a single `tool_result` with a combined id",
          "isCorrect": false
        }
      ],
      "tags": ["tool-use", "parallel-tools"],
      "hints": ["Count the user messages, then count the tool_result blocks."]
    },
    {
      "id": "ccaf1-quiz-012",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Your agent must return a strict JSON verdict at the end of its run. What is the most reliable way to get it?",
      "answer": "Give the model a dedicated `submit_result` tool whose `input_schema` is the verdict shape. Completion then arrives as a validated `tool_use` block instead of prose you have to parse, which also removes the need for a natural-language termination sentinel.",
      "choices": [
        {
          "id": "a",
          "text": "Define a `submit_result` tool whose input schema is the verdict, and treat its call as completion",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Ask in the system prompt for JSON only and parse the final text block",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Instruct the model to end with the sentinel `<<<JSON>>>` and split on it",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Lower the temperature to 0 so the JSON is always valid",
          "isCorrect": false
        }
      ],
      "tags": ["structured-output", "termination", "tool-use"],
      "hints": ["Prose you have to parse, versus a schema the API validates for you."]
    },
    {
      "id": "ccaf1-quiz-013",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Which statement about the Messages API and conversation state is correct?",
      "answer": "The API is stateless: every request must carry the full `messages` array. Persistence, trimming, and compaction are the application's responsibility.",
      "choices": [
        {
          "id": "a",
          "text": "The API is stateless - your application must resend the whole conversation each turn",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "The API keeps a server-side session keyed by conversation id",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Only the last two turns need to be resent; the rest is cached server-side",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Prompt caching stores conversation state so history can be omitted",
          "isCorrect": false
        }
      ],
      "tags": ["state", "messages-api"],
      "hints": ["Ask what the API remembers between two consecutive calls."]
    },
    {
      "id": "ccaf1-quiz-014",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Your orchestrator's context window fills up because every worker returns its full raw research output. What is the best fix?",
      "answer": "Have each worker return a compact, structured summary. The whole point of a subagent is that it absorbs the bulk reading in its own context and hands back only the distilled result.",
      "choices": [
        {
          "id": "a",
          "text": "Have each worker return a compact structured summary instead of raw output",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Increase the orchestrator's `max_tokens`",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Run the workers sequentially instead of in parallel",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Move the worker outputs into the system prompt",
          "isCorrect": false
        }
      ],
      "tags": ["multi-agent", "context", "subagents"],
      "hints": ["Why do subagents exist in the first place?"]
    },
    {
      "id": "ccaf1-quiz-015",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A refund agent can issue refunds up to any amount. Compliance requires human approval above $500. What is the cleanest design?",
      "answer": "Keep the check in your code, where it is enforceable. The model's job is to propose; the application decides whether the proposal executes or goes to an approval queue. A prompt instruction is guidance, not enforcement.",
      "choices": [
        {
          "id": "a",
          "text": "Validate the amount in the tool implementation and route anything over $500 to an approval queue",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Add 'never refund more than $500 without approval' to the system prompt",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Put a `max: 500` constraint in the tool's JSON schema and rely on it",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Use a lower temperature so the model is more conservative with amounts",
          "isCorrect": false
        }
      ],
      "tags": ["human-in-the-loop", "guardrails", "security"],
      "hints": ["Which one of these is enforcement, and which are suggestions?"]
    },
    {
      "id": "ccaf1-quiz-016",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A research report reads as complete, but two of the five subtopics were never actually covered. What should the coordinator do?",
      "answer": "This is the iterative refinement loop: the coordinator is the only participant that can see the whole picture, so it evaluates coverage, re-delegates targeted queries for the gaps, and re-invokes synthesis until coverage is sufficient.",
      "choices": [
        {
          "id": "a",
          "text": "Evaluate the synthesis for gaps, re-delegate targeted queries for those subtopics, then re-synthesize",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Instruct the synthesis subagent to state its confidence in each section of the report",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Increase the number of parallel search subagents so more sources get covered per run",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Have the report subagent flag any section shorter than the others as likely incomplete",
          "isCorrect": false
        }
      ],
      "tags": ["multi-agent", "iterative-refinement"],
      "hints": ["Length and self-reported confidence are not coverage."]
    },
    {
      "id": "ccaf1-quiz-017",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "The agent picks the right tool but keeps passing malformed arguments. What does the guide point at?",
      "answer": "Tool descriptions are the primary mechanism the model uses, and they should carry input formats, example queries, edge cases, and boundary explanations. Right tool with wrong arguments is a description problem, not a prompt or model problem.",
      "choices": [
        {
          "id": "a",
          "text": "The tool description, which should state input formats, example queries, and edge cases",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "The system prompt, which should repeat the argument format for each available tool",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "The model tier, which should be raised so argument construction becomes more reliable",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "The retry logic, which should re-issue the call with the arguments normalized in code",
          "isCorrect": false
        }
      ],
      "tags": ["tool-design"],
      "hints": [
        "Right tool, wrong arguments. Which artifact was supposed to explain the arguments?"
      ]
    },
    {
      "id": "ccaf1-quiz-018",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Your coordinator's prompt tells it to delegate research to subagents, but it never spawns one. What is most likely missing?",
      "answer": "The Task tool is the mechanism for spawning subagents, and a coordinator can only invoke them if `allowedTools` includes `\"Task\"`. No amount of prompting substitutes for the missing permission.",
      "choices": [
        {
          "id": "a",
          "text": "`\"Task\"` is absent from the coordinator's `allowedTools`, so it cannot spawn subagents",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "The subagent definitions lack `description` fields, so the coordinator cannot match them",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "The coordinator is emitting its Task calls across separate turns instead of one response",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "The subagents inherit no parent context, so they return nothing for the coordinator to use",
          "isCorrect": false
        }
      ],
      "tags": ["multi-agent", "task-tool", "allowed-tools"],
      "hints": ["Delegation is a tool call. Is that tool allowed?"]
    },
    {
      "id": "ccaf1-quiz-019",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A support agent must never issue a refund before the customer's identity is verified. What design guarantees that?",
      "answer": "When deterministic compliance is required - identity verification before a financial operation - prompt instructions have a non-zero failure rate. The prerequisite gate blocks the downstream tool call until the prerequisite step has completed.",
      "choices": [
        {
          "id": "a",
          "text": "A programmatic prerequisite that blocks `process_refund` until `get_customer` returned a verified id",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "A system prompt rule stating that `get_customer` must always be called before any refund",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "A tool description on `process_refund` explaining that verification is required beforehand",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "A few-shot example showing the correct call order for a refund on a verified account",
          "isCorrect": false
        }
      ],
      "tags": ["enforcement", "human-in-the-loop", "scenario-support"],
      "hints": ["Three of these are ways of asking. One is a way of preventing."]
    },
    {
      "id": "ccaf1-quiz-020",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Which of these is NOT one of the three agentic anti-patterns the exam emphasizes?",
      "answer": "Returning `is_error: true` on a failed tool is the *recommended* practice, not an anti-pattern. The three anti-patterns are natural-language termination, arbitrary iteration caps used as the termination strategy, and treating text content as a completion signal.",
      "choices": [
        {
          "id": "a",
          "text": "Returning `is_error: true` in a `tool_result` when a tool fails",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Stopping the loop when the model writes a termination phrase",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Using an arbitrary iteration cap as the way the loop decides it is done",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Treating the presence of a text block as the completion signal",
          "isCorrect": false
        }
      ],
      "tags": ["anti-patterns"],
      "hints": ["Three of these are mistakes. One is the documented recommendation."]
    },
    {
      "id": "ccaf1-type-001",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "Which response field should drive an agentic loop's continue/stop decision?",
      "answer": "`stop_reason` - the typed control signal on every Messages API response.",
      "shortAnswer": "stop_reason",
      "tags": ["agentic-loop"],
      "hints": ["It sits on every response, right next to the content array."]
    },
    {
      "id": "ccaf1-type-002",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "Which `stop_reason` value means the model wants you to execute a tool?",
      "answer": "`tool_use`. Execute the requested tools, then append the results as `tool_result` blocks and re-invoke.",
      "shortAnswer": "tool_use",
      "tags": ["stop-reason"],
      "hints": ["It is named after the block type the model just emitted."]
    },
    {
      "id": "ccaf1-type-003",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "Which `stop_reason` value means the model finished its response naturally?",
      "answer": "`end_turn` - the most common value; use the response as-is.",
      "shortAnswer": "end_turn",
      "tags": ["stop-reason"],
      "hints": ["Two words joined by an underscore - the most common value of all."]
    },
    {
      "id": "ccaf1-type-004",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "Which field on a `tool_result` block links it to the `tool_use` block it answers?",
      "answer": "`tool_use_id`. It must exactly match the `id` of the `tool_use` block from the assistant turn.",
      "shortAnswer": "tool_use_id",
      "tags": ["tool-use"],
      "hints": ["It has to match the id of the block it is answering."]
    },
    {
      "id": "ccaf1-type-005",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "Which boolean flag on a `tool_result` tells the model the tool call failed?",
      "answer": "`is_error`. Set it to `true` and put a short, actionable message in `content`.",
      "shortAnswer": "is_error",
      "tags": ["error-handling"],
      "hints": ["Two words with an underscore, and its value is a boolean."]
    },
    {
      "id": "ccaf1-type-006",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "What is the name of the topology where one coordinator manages all inter-subagent communication?",
      "answer": "Hub-and-spoke. The coordinator is the hub; subagents are spokes and never talk to each other, which is what gives you observability, consistent error handling, and controlled information flow.",
      "shortAnswer": "hub-and-spoke",
      "tags": ["multi-agent", "hub-and-spoke"],
      "hints": ["A wheel: one centre, many spokes, no rim between them."]
    },
    {
      "id": "ccaf1-match-001",
      "subjectId": "ccaf-1-agentic",
      "type": "match",
      "question": "Match each agent-loop signal to the role it should play.",
      "answer": "The loop's control flow rests on `stop_reason`, and the guide names exactly two values for it: `tool_use` continues, `end_turn` terminates. The other three rows are the three anti-patterns the exam calls out by name.",
      "matchPairs": [
        {
          "left": "stop_reason is \"tool_use\"",
          "right": "Execute the tools and continue the loop"
        },
        {
          "left": "stop_reason is \"end_turn\"",
          "right": "Terminate the loop and present the response"
        },
        {
          "left": "An iteration cap",
          "right": "Emergency backstop - never the primary stopping mechanism"
        },
        {
          "left": "The response contains a text block",
          "right": "Not a completion indicator - checking it is an anti-pattern"
        },
        {
          "left": "The model wrote a sentinel word",
          "right": "Natural-language termination - the classic anti-pattern"
        }
      ],
      "tags": ["agentic-loop", "anti-patterns"],
      "hints": [
        "Two of these are the mechanism. Three are mistakes, one of which is still worth having."
      ]
    },
    {
      "id": "ccaf1-match-002",
      "subjectId": "ccaf-1-agentic",
      "type": "match",
      "question": "Match each decomposition strategy to the work it fits.",
      "answer": "Two axes decide it: are the steps knowable up front, and are the subtasks independent? Parallelism is expressed concretely - the coordinator emits several Task tool calls in a single response rather than across separate turns.",
      "matchPairs": [
        {
          "left": "Fixed sequential pipeline",
          "right": "Predictable multi-aspect review with known steps"
        },
        {
          "left": "Dynamic adaptive decomposition",
          "right": "Open-ended investigation that reveals its own subtasks"
        },
        {
          "left": "Per-file passes plus an integration pass",
          "right": "A large multi-file review prone to attention dilution"
        },
        {
          "left": "Multiple Task calls in one response",
          "right": "Independent subtasks that should run in parallel"
        },
        {
          "left": "Coordinator selects a subset",
          "right": "A simple query that does not need the full pipeline"
        }
      ],
      "tags": ["task-decomposition", "multi-agent"],
      "hints": [
        "Sort by two questions: are the steps knowable up front, and are the subtasks independent?"
      ]
    },
    {
      "id": "ccaf1-match-003",
      "subjectId": "ccaf-1-agentic",
      "type": "match",
      "question": "Match each agentic anti-pattern to the correct alternative.",
      "answer": "All three anti-patterns share one root cause: inferring control flow from unstructured output instead of using the API's typed signals.",
      "matchPairs": [
        {
          "left": "Stop when the model writes DONE",
          "right": "Branch on stop_reason == end_turn"
        },
        {
          "left": "Loop exactly N times and return",
          "right": "Loop on stop_reason; keep N only as a circuit breaker that escalates"
        },
        {
          "left": "Text block present means finished",
          "right": "A turn can hold text and tool_use together - check stop_reason"
        },
        {
          "left": "Parse the final answer out of prose",
          "right": "Have the model call a submit_result tool with a JSON schema"
        }
      ],
      "tags": ["anti-patterns"],
      "hints": ["Every correct fix replaces an inference from text with a typed signal."]
    },
    {
      "id": "ccaf1-match-004",
      "subjectId": "ccaf-1-agentic",
      "type": "match",
      "question": "Match each agentic-loop failure to its correct handling.",
      "answer": "Each of these has a distinct correct path; conflating them is a common production bug.",
      "matchPairs": [
        {
          "left": "Tool raises a timeout",
          "right": "tool_result with is_error: true and a short message"
        },
        {
          "left": "stop_reason == max_tokens mid tool call",
          "right": "Do not execute; retry with a larger budget or continue the turn"
        },
        {
          "left": "Circuit breaker trips at max iterations",
          "right": "Treat as failure, log it, escalate to a human"
        },
        {
          "left": "Model asks for an action above the policy threshold",
          "right": "Queue it for human approval before executing"
        }
      ],
      "tags": ["error-handling", "reliability"],
      "hints": ["One of these is not the model’s problem to solve - it leaves the loop."]
    },
    {
      "id": "ccaf1-match-005",
      "subjectId": "ccaf-1-agentic",
      "type": "match",
      "question": "Match each agent-loop responsibility to what actually owns it.",
      "answer": "The dividing line: the model decides *what* should happen next; your code decides *whether it happens* and enforces every rule that matters. Termination is owned by neither opinion - it is a field on the response.",
      "matchPairs": [
        {
          "left": "Choosing which tool to call next",
          "right": "The model, from the tools you expose"
        },
        {
          "left": "Executing the call and handling its failure",
          "right": "Your application code"
        },
        {
          "left": "Enforcing the refund approval threshold",
          "right": "A server-side check inside the tool"
        },
        {
          "left": "Carrying the conversation across turns",
          "right": "Your datastore, keyed by session id"
        },
        {
          "left": "Deciding the turn is over",
          "right": "The stop_reason field on the response"
        }
      ],
      "tags": ["architecture", "guardrails"],
      "hints": [
        "The model proposes, something else disposes - and termination is owned by neither."
      ]
    },
    {
      "id": "ccaf1-open-101",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "How does a coordinator spawn subagents, and what does a subagent start with?",
      "answer": "**The mechanism** is the **Task tool**, and the prerequisite catches people: the\ncoordinator's **`allowedTools` must include `\"Task\"`** or it cannot invoke a subagent at all. No\namount of prompting substitutes for the missing permission - the agent will describe delegating\nand then do the work itself.\n\n**What a subagent starts with: nothing.** It does not inherit the coordinator's conversation\nhistory, and there is no shared memory between invocations. Everything it needs goes **explicitly\ninto its prompt** - the synthesis subagent gets the web search results and the document analysis\noutput handed to it, or it synthesizes from thin air.\n\nPass that context as **structured data separating content from metadata** - source URLs, document\nnames, page numbers - so attribution survives the hop. Prose summaries lose the provenance at\nexactly the point where the original material stops existing.\n\n**Parallelism has a specific shape**: emit **multiple Task calls in a single coordinator\nresponse**, not one per turn across several turns. Sequential turns serialize work that had no\ndependency between the branches.\n\n**AgentDefinition** carries each subagent type's configuration: `description`, system prompt, and\n**tool restrictions**. The description is what gets a subagent selected for the right work; the\ntool restrictions are what stop it from wandering into another's specialization.",
      "tags": ["multi-agent", "task-tool", "context-passing"],
      "hints": ["One permission, one prompt payload, and one detail about how parallel is spelled."]
    },
    {
      "id": "ccaf1-quiz-101",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "The synthesis subagent produces vague conclusions that ignore what the search subagent found. What is the likely cause?",
      "answer": "Subagent context must be explicitly provided in the prompt - subagents do not automatically inherit parent context or share memory between invocations. Complete findings from prior agents go directly into the prompt of the one that needs them.",
      "choices": [
        {
          "id": "a",
          "text": "Its prompt never carried the search findings, since subagents inherit no parent context",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "The coordinator invoked it before the search subagent had finished its own work",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Its context window is too small to hold the volume the search subagent returned",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Its system prompt lacks synthesis instructions specific to the research domain",
          "isCorrect": false
        }
      ],
      "tags": ["multi-agent", "context-passing"],
      "hints": ["It is not that it saw too much or too little. It saw nothing."]
    },
    {
      "id": "ccaf1-quiz-102",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Five independent research branches run one after another and the query takes far too long. What is the fix?",
      "answer": "Spawning parallel subagents means emitting multiple Task tool calls in a single coordinator response rather than across separate turns. Dispatching one per turn is what serializes independent work.",
      "choices": [
        {
          "id": "a",
          "text": "Emit all five Task calls in a single coordinator response",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Raise the coordinator's turn limit so it can dispatch the branches more quickly",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Merge the five branches into one subagent that handles every subtopic in sequence",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Give each subagent a faster model so each individual branch finishes sooner",
          "isCorrect": false
        }
      ],
      "tags": ["multi-agent", "task-tool", "parallelism"],
      "hints": ["The dispatch pattern is the bug, not the model or the limits."]
    },
    {
      "id": "ccaf1-quiz-103",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "How should a coordinator prompt be written so subagents handle unexpected material well?",
      "answer": "Coordinator prompts that specify research goals and quality criteria rather than step-by-step procedural instructions are what enable subagent adaptability. Rigid procedure produces identical behaviour that breaks the moment reality differs from the script.",
      "choices": [
        {
          "id": "a",
          "text": "State the research goals and quality criteria rather than step-by-step procedure",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Give exact procedural steps, so every subagent behaves identically across runs",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Give the goals plus a fallback procedure for when the goals cannot be met",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Give minimal instruction, since a capable subagent infers the goal from the query",
          "isCorrect": false
        }
      ],
      "tags": ["multi-agent", "prompting"],
      "hints": ["Procedure is only identical until something unexpected shows up."]
    },
    {
      "id": "ccaf1-type-101",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "Which tool must be in a coordinator's `allowedTools` for it to spawn subagents?",
      "answer": "The `Task` tool. It is the spawning mechanism, and without it in `allowedTools` the coordinator simply cannot delegate.",
      "shortAnswer": "Task",
      "tags": ["multi-agent", "task-tool"],
      "hints": ["Four letters, capitalized, and it is the name of the thing being handed off."]
    },
    {
      "id": "ccaf1-open-102",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "What does a well-designed escalation handoff contain, and why?",
      "answer": "The constraint that determines the whole design: **the human agent does not have access to\nthe conversation transcript.** Everything they need to act has to be in the handoff itself.\n\nSo the handoff is a **structured summary**, not a pointer:\n\n- **Customer id** - who this is, already verified\n- **Root cause analysis** - what actually went wrong, not just what was reported\n- **The relevant figures** - refund amount, order number, dates\n- **Recommended action** - what the agent would have done, so the human is reviewing a proposal\n  rather than starting an investigation\n\nThe failure this prevents is the one every customer has experienced: being transferred and asked\nto explain the entire problem again. An agent that escalates without context has spent the\ncustomer's time and handed the human a blank page.\n\n**Where this sits in the loop**: escalation is a **tool** the agent calls, not a phrase it writes,\nbecause it has to carry a structured payload and trigger something in your system.\n\nThe related pattern for complex tickets: **decompose a multi-concern request** into distinct\nitems, investigate each **in parallel using shared context**, then synthesize one unified\nresolution - rather than answering the first concern and losing the other two.",
      "tags": ["escalation", "handoff", "scenario-support"],
      "hints": ["Ask what the human on the other end can see. The answer is nothing."]
    },
    {
      "id": "ccaf1-quiz-104",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A customer message raises three separate issues in one paragraph. What is the recommended handling?",
      "answer": "Decomposing multi-concern customer requests into distinct items, investigating each in parallel using shared context, then synthesizing a unified resolution is the named skill. Sequential handling is slower with no benefit when the concerns are independent.",
      "choices": [
        {
          "id": "a",
          "text": "Decompose into distinct items, investigate each in parallel with shared context, then unify",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Answer the clearest issue first and ask the customer to resubmit the others",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Escalate to a human, since multi-concern requests exceed the agent's design",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Handle the three sequentially, so each investigation sees the previous outcome",
          "isCorrect": false
        }
      ],
      "tags": ["scenario-support", "task-decomposition"],
      "hints": ["Three issues, one customer, and no reason for them to queue."]
    },
    {
      "id": "ccaf1-quiz-105",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "Which statement about hooks versus prompt instructions is correct?",
      "answer": "The distinction is between deterministic guarantees and probabilistic compliance. When business rules require guaranteed compliance, the enforcement goes in a hook, because the model is not in the path at all.",
      "choices": [
        {
          "id": "a",
          "text": "Hooks give deterministic guarantees; prompt instructions give probabilistic compliance",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Hooks and prompts are equivalent once the instruction is stated clearly enough",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Hooks are for formatting concerns; business rules belong in the system prompt",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Prompt instructions are deterministic when the model runs at temperature zero",
          "isCorrect": false
        }
      ],
      "tags": ["hooks", "enforcement"],
      "hints": ["One of these two has a non-zero failure rate by construction."]
    },
    {
      "id": "ccaf1-type-102",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "Which hook pattern intercepts tool results so they can be transformed before the model reads them?",
      "answer": "`PostToolUse`. The canonical use is normalizing heterogeneous formats - Unix timestamps, ISO 8601, numeric status codes - from different MCP tools before the agent has to reason about them.",
      "shortAnswer": "PostToolUse",
      "tags": ["hooks", "agent-sdk"],
      "hints": ["It runs after the tool, before the model."]
    },
    {
      "id": "ccaf1-quiz-106",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "A code review over many files produces shallow findings and misses issues that span files. How should the work be decomposed?",
      "answer": "Splitting large reviews into per-file local passes plus a separate cross-file integration pass avoids attention dilution. The single combined pass is what produced the shallow findings.",
      "choices": [
        {
          "id": "a",
          "text": "Per-file local analysis passes, plus a separate cross-file integration pass",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "One pass per issue category, so each run has a single kind of thing to look for",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "One pass over all files at once, with the prompt asking for cross-file analysis",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "One pass per directory, so related files are always reviewed alongside each other",
          "isCorrect": false
        }
      ],
      "tags": ["task-decomposition", "multi-pass-review"],
      "hints": ["Two different searches are being asked of one pass."]
    },
    {
      "id": "ccaf1-match-101",
      "subjectId": "ccaf-1-agentic",
      "type": "match",
      "question": "Match each coordination decision to what it controls.",
      "answer": "Each row is a concrete configuration choice with a behavioural consequence. Three of the five are things a prompt cannot compensate for once they are wrong.",
      "matchPairs": [
        {
          "left": "allowedTools includes \"Task\"",
          "right": "Whether the coordinator can delegate at all"
        },
        {
          "left": "Multiple Task calls in one response",
          "right": "Whether independent branches run in parallel"
        },
        {
          "left": "Findings pasted into the subagent prompt",
          "right": "Whether the subagent knows anything at all"
        },
        {
          "left": "AgentDefinition tool restrictions",
          "right": "Whether a subagent can stray from its role"
        },
        {
          "left": "Goals and criteria instead of procedure",
          "right": "Whether a subagent adapts to the unexpected"
        }
      ],
      "tags": ["multi-agent", "task-tool"],
      "hints": ["Some of these are permissions, some are payload, one is prompt style."]
    },
    {
      "id": "ccaf1-open-103",
      "subjectId": "ccaf-1-agentic",
      "type": "open",
      "question": "When do you resume a session, when do you fork it, and when do you start fresh?",
      "answer": "**`--resume <session-name>`** continues a specific named prior conversation. Use it when the\nprior context is **mostly still valid**.\n\n**`fork_session`** branches from a shared baseline into independent explorations. The use case is\ndivergent approaches from common ground: analyze the codebase once, then fork to compare two\ntesting strategies or two refactoring approaches without paying for the analysis twice - and\nwithout either branch contaminating the other.\n\n**Starting fresh with an injected summary** is the one people under-use, and the guide is explicit\nthat it is **more reliable than resuming with stale tool results**. A resumed session carries file\ncontents that may no longer be true; a fresh session with a structured summary of what was learned\ncarries the conclusions without the rot.\n\nSo the decision is about **staleness**, not convenience:\n\n| Prior context | Do |\n|---|---|\n| Mostly valid | `--resume` |\n| Valid, but you want two divergent paths | `fork_session` |\n| Tool results now stale | Fresh session + structured summary |\n\n**When you do resume after code changed**, tell the agent **which files changed**. That triggers\ntargeted re-analysis instead of either full re-exploration - expensive - or silent reliance on a\nstale reading of the file, which is worse.",
      "tags": ["sessions", "fork-session", "resume"],
      "hints": ["The deciding question is not what you want to keep. It is what has gone stale."]
    },
    {
      "id": "ccaf1-quiz-107",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "You analyzed a codebase yesterday, then several of the analyzed files were rewritten. What is the most reliable move?",
      "answer": "Starting a new session with a structured summary is more reliable than resuming with stale tool results. Resuming carries file contents that are no longer true, and the agent has no way to know which of its recollections have expired.",
      "choices": [
        {
          "id": "a",
          "text": "Start a fresh session with a structured summary of what the analysis concluded",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "Resume the session, since its analysis of the unchanged files is still accurate",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Resume the session and ask the agent to re-read every file it looked at before",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "Fork the session, keeping the original as a record of the previous analysis",
          "isCorrect": false
        }
      ],
      "tags": ["sessions", "resume"],
      "hints": ["The old session is not wrong about everything. That is what makes it dangerous."]
    },
    {
      "id": "ccaf1-quiz-108",
      "subjectId": "ccaf-1-agentic",
      "type": "quiz",
      "question": "You want to compare two refactoring approaches without re-analyzing the codebase for each. What do you use?",
      "answer": "fork_session creates independent branches from a shared analysis baseline for exploring divergent approaches. Sequential resumption lets the first approach's reasoning bleed into the second; one session holding both invites the same contamination.",
      "choices": [
        {
          "id": "a",
          "text": "`fork_session`, branching both explorations from the shared analysis",
          "isCorrect": true
        },
        {
          "id": "b",
          "text": "`--resume` twice, running the two approaches one after the other in that session",
          "isCorrect": false
        },
        {
          "id": "c",
          "text": "Two fresh sessions, each given the same structured summary of the analysis",
          "isCorrect": false
        },
        {
          "id": "d",
          "text": "One session, asking the agent to hold both approaches and compare them at the end",
          "isCorrect": false
        }
      ],
      "tags": ["sessions", "fork-session"],
      "hints": ["You want a common starting point and no cross-talk after it."]
    },
    {
      "id": "ccaf1-type-103",
      "subjectId": "ccaf-1-agentic",
      "type": "type-answer",
      "question": "Which flag continues a specific named prior Claude Code conversation?",
      "answer": "`--resume <session-name>`. Pair it with telling the agent which files changed since, so it re-analyzes those instead of trusting stale tool results.",
      "shortAnswer": "--resume",
      "tags": ["sessions", "resume"],
      "hints": ["Two dashes and the obvious verb."]
    },
    {
      "id": "ccaf1-match-102",
      "subjectId": "ccaf-1-agentic",
      "type": "match",
      "question": "Match each session situation to the right mechanism.",
      "answer": "Four of these are about staleness; the fifth is about volume. Mixing them up is how a session gets compacted when it should have been abandoned.",
      "matchPairs": [
        {
          "left": "Prior context is mostly still valid",
          "right": "--resume with the session name"
        },
        {
          "left": "Two approaches from one shared analysis",
          "right": "fork_session"
        },
        {
          "left": "Prior tool results have gone stale",
          "right": "Fresh session with an injected summary"
        },
        {
          "left": "Some analyzed files changed since",
          "right": "Resume, naming the changed files"
        },
        {
          "left": "Context filled with verbose discovery",
          "right": "/compact"
        }
      ],
      "tags": ["sessions"],
      "hints": ["Four rows share one question. The fifth asks something different."]
    }
  ]
}
